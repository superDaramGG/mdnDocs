const alphabets = ['a','a','a','b','c','c','d','e'];
alphabets.reduce((acc, current) => {
  if (acc[current]) {
    //current에 a가 오면
    acc[current] += 1;
  } else {
    acc[current] = 1;
  }
  return acc;
}, {})

console.log(counts);
